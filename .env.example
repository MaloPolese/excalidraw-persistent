# Board password — required, anyone with the link will need this to access the board
BOARD_PASSWORD=your-password-here

# Secret for signing the session cookie — generate with: openssl rand -hex 32
SECRET=your-secret-here
BACKUP_CRON=0 2 * * * # Every day at 2am